基于OpenAI SDK兼容调用大模型API的实用工具，支持阿里云百炼（通义千问）和OpenAI官方大模型，具备配置分离、异常处理、可复用等特性。

## 功能
1.  配置分离：使用环境变量管理敏感信息（比如API Key），避免硬编码；
2.  异常处理：覆盖认证失败、频率超限、API错误等常见场景；
3.  兼容扩展：无需修改核心逻辑，仅修改配置即可切换不同厂商大模型；
4.  可复用性：封装成函数，方便后续项目导入调用。
